import Home from "@/components/home";

export const metadata = {
  title: "Home — NutritiScan AI",
  description: "Where you are today, and the one thing to do next.",
};

export default function Page() {
  return <Home />;
}
