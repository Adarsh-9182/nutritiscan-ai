import ProfileScreen from "@/components/profile-screen";

export const metadata = {
  title: "Profile — NutritiScan AI",
  description: "Everything your AI remembers about you — body, goal, diet, allergies, conditions and labs.",
};

export default function Page() {
  return <ProfileScreen />;
}
